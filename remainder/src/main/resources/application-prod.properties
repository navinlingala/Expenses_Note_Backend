# ===================================================================
# PROD PROFILE (Neon PostgreSQL + Cloud Hosting)
# ===================================================================
server.port=${PORT:8080}

# Production Neon Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-damp-wildflower-b3xehppc-pooler.c-4.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_zpNPgrZn4H1E}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Production Settings (Optimized, no verbose sql in prod logs)
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=update
